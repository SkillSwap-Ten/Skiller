'use client'

// This page is rendered inside the layout.
// On desktop, the layout handles showing either the empty state or the ChatWindow via state.
// On mobile, this shows nothing visible because the sidebar takes full width.
// The layout's MainContent is hidden on mobile when no /chat/[id] route is active.

export default function UserSocial() {
  return null
}